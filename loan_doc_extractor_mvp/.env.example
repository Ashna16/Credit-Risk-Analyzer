# Copy to .env in this folder. Optional: full AI extraction uses Google Gemini.
# Without GEMINI_API_KEY, the app uses local pattern-based extraction (still runs).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
